Les mappers permettent de prendre un tableeau de donn�e en entr�e venant d'un reader
et de mapper ces donn�es sur un autre tableau ( ou objet ? ) en suivant 
 - des regles de nommage ( le champ "titre" devient "sujet", "description" devient "body" etc )
 - des regles de transformation (le champ "nom" est mis en majuscule )


les regles sont stocket dans un fichier de config yaml ? ou en base de donn�e ?

on peut prevoir une interface d'admin pour générer ses regles ;) (admin developper par adrien pour typo3 )

 